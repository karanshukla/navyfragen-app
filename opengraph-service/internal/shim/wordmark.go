package shim

// AppNameWordmarkHTML is the footer's two-tone lockup — first syllable in the
// body colour, second in {{TEXT_ACCENT}} — split by design rather than derived
// from AppName.
const AppNameWordmarkHTML = `navy<span>fragen</span>`
