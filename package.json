{
    "name": "navyfragen-monorepo",
    "private": true,
    "workspaces": [
        "client",
        "server"
    ],
    "scripts": {
        "dev": "concurrently \"npm:dev --workspace=client\" \"npm:dev --workspace=server\""
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    },
    "dependencies": {
        "@types/ws": "^8.18.1",
        "node-fetch": "^2.7.0",
        "ws": "^8.18.2"
    }
}
